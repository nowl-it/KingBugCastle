## Server Restart Rule

Mỗi lần sửa server local (ví dụ như code Python trong thư mục server/), bạn hãy báo cho NGƯỜI DÙNG biết để họ tự restart lại process `uvicorn` ở terminal của họ. Không tự ý chạy script restart vì không có script nào tên `run.sh` cả.
